# Self-host dashboard — user guide

This is the browser dashboard for the self-host API. Open `/connect`, enter `http://localhost:8000` and the same `INSTANCE_SECRET` as the API, then view servers, metrics, and analytics as your wrapped MCP tools run. Same data as the desktop app; not the hosted Vercel dashboard.
