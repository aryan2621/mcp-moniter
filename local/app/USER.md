# Desktop app — user guide

Desktop window around the same dashboard as [../ui](../ui). Connect to a running local API the same way (`API URL` + `INSTANCE_SECRET`) and view the same tool-call metrics. No extra backend. This is not the hosted Vercel app.
