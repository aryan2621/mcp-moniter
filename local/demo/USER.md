# Self-host demos — user guide

Sample MCP servers that send tool-call metrics to your local API. Start the stack, connect the dashboard with `INSTANCE_SECRET`, set `MCP_MONITOR_SECRET` to that same value, run a demo, then watch servers and metrics in the local UI. These demos do not use the hosted Vercel app.
